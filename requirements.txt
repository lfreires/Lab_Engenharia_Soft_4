# Dependências de runtime — geradas a partir de pyproject.toml
# Instalar: pip install -r requirements.txt
# Ou instalar o projeto editável: pip install -e ".[dev]"

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
bcrypt>=4.2
pyjwt>=2.9
sqlalchemy>=2.0,<3.0
alembic>=1.14
psycopg[binary]>=3.2
google-cloud-firestore>=2.21
